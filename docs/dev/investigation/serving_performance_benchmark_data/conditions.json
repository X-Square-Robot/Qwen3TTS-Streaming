{
  "date_utc": "2026-07-08",
  "hardware": {
    "gpu": "NVIDIA GeForce RTX 5090",
    "gpu_memory_mib": 32607,
    "driver_version": "580.126.20"
  },
  "software": {
    "engine_image": "qwen3-engine:25.10 (NGC 25.10 base nvcr.io/nvidia/tensorrt:25.10-py3, verified via image env NVIDIA_TENSORRT_VERSION=25.10; rebuilt 2026-07-08, full-bf16)",
    "triton_image": "qwen3tts-streaming:25.10 (Triton dataset from 2026-07-06, see dataset_provenance)",
    "tensorrt_version": "10.13.3.9",
    "ngc_tag": "25.10",
    "cuda_version": "13.0.2.006",
    "engine_optimizations_included": [
      "ff8dc4f feat(export): CP 15-stage unroll keeps KV cache (~3x decode compute reduction)",
      "c9c63c2 perf(engine): CUDA-graph decode replay (eliminates ~2700 kernel launches/step of CPU cost)",
      "dd647be fix(engine): batch KV gather arena-ized (removes up-to-7.5GiB transient allocation per eager decode step)",
      "a41f35d fix(engine): queue_wait/cache_hit metric wiring",
      "d5741b6 fix(triton): ServerTimingAccumulator wiring + end->done terminal event unification",
      "b80cf45 perf(engine): batched burst admission + Phase-1 per-slot tax elimination (c2w state arenas)",
      "2a9578b perf(engine,gateway): c2w KV sliding window pooled per slot + serving hot-path slimming (async logging, orjson, gRPC chunk coalescing, uvloop)",
      "a8e7274 perf(gateway): VAD-disabled sessions bypass the per-chunk int16 round-trip (also removes a hidden 15-bit quantization for pcm_f32)",
      "cc58c6a fix(engine,gateway): post-review audit fix batch (ghost-trailing crash, prefill staging aliasing, per-session delivery isolation, chunk coalescing actually taking effect)",
      "8596996 perf(engine): batched p3_launch (bit-exact), full-width decode iteration 48 -> 41.5 ms",
      "b80c38d perf(server): session open only offloads to a thread when ref-audio feature extraction is needed"
    ],
    "runtime_note": "This dataset was re-collected 2026-07-08 on a fresh full-bf16 rebuild at repo HEAD 7db42d7. The two commits since the b80c38d baseline (9ad7cb0, 7db42d7) change only build-time precision defaults and docs, not engine runtime code, so the runtime is identical to the b80c38d baseline; the refreshed engine numbers reproduce the 2026-07-08 (ca6ddb9) dataset within trial-to-trial variance."
  },
  "dataset_provenance": {
    "engine_rows": "re-collected 2026-07-08 on the full-bf16 rebuild above; trials bf16refresh-engine-01..03 (this refresh reproduces the prior ca6ddb9 engine dataset within noise)",
    "triton_rows": "retained from the 2026-07-06 dataset (engine core WITHOUT b80cf45 and every later optimization commit); trials triton-01..03. The Triton BLS path wraps the same engine core, so the engine-side commits would improve it too -- its rows OVERSTATE current Triton latency and are kept only as the last measured reference.",
    "note": "raw_requests.csv = one representative engine trial (bf16refresh-engine-01, 2026-07-08) + one representative Triton trial (triton-01, 2026-07-06)"
  },
  "metric_semantics_change": {
    "prefill_ms": "As of b80cf45 (batched burst admission), prefill_started/prefill_completed bracket the whole batched admission pass (slot allocation + batched prefix-cache restore + batched suffix embed for every session admitted in that pass), not one session's own suffix compute. Under burst load prefill_ms therefore reads as tens of ms per session (engine rows) even though per-session GPU work is unchanged; at concurrency 1 the pass contains a single session and the value is comparable to the old semantics (~1ms). Triton rows predate the change and use the old per-session semantics. prefill_ms values are NOT comparable across the two datasets at load."
  },
  "model": {
    "variant": "custom-1.7b",
    "loaded_model_type": "custom_voice",
    "engine_mode": "trt",
    "backbone_precision": "bf16",
    "cp_precision": "bf16",
    "code2wav_precision": "bf16",
    "engine_dtype": "bf16",
    "triton_io_float_dtype": "bf16",
    "note": "Full-bf16 engine (the shipped default). cp=bf16 validated hallucination-free on the internal 0701 checkpoint (0/100 deterministic probe; see docs/dev/investigation/streaming_hallucination.md). Re-probed 0/100 on 2026-07-08 on this exact rebuild immediately before this dataset was collected (runs=100, duration min 9.36s / median 10.08s / max 10.88s). A code2wav=fp16 variant was also evaluated 2026-07-08 and rejected as the default: it speeds single-stream decode (-21% at c1) but regresses the b128 decode step (+8%, crossover ~c32); kept as a low-concurrency opt-in (see engine_overview.md 1.5)."
  },
  "engine_profile": {
    "max_batch_size": 128,
    "max_input_len": 128,
    "max_seq_len": 512,
    "max_sessions": 128,
    "max_queue_size": 256,
    "note": "batch=128 selected automatically by the export-aware profile sizing (raw capacity ~141 lanes on this 32GiB card). The batch profile is a TEST VARIABLE, not a hardware ceiling -- see the report's 'Batch profile as a variable' section."
  },
  "benchmark_request": {
    "task_type": "custom_voice",
    "speaker": "Serena",
    "language": "auto",
    "text": "你好，欢迎使用 Qwen3-TTS 语音合成系统，这是一段用于压测的示例文本。",
    "note": "Fixed text/speaker across every request: isolates protocol/concurrency effects, and pins the run to a ~100% prefix-cache-hit regime (verified: cache_hit=True on 100% of measured rows). All prefill numbers are cache-hit (suffix-only) prefill."
  },
  "endpoints": {
    "engine-grpc": "localhost:50051",
    "engine-websocket": "ws://localhost:50052/v1/ws",
    "triton-grpc": "localhost:8001"
  },
  "isolation": {
    "policy": "single-service: Triton container stopped during engine-path trials, engine container stopped during Triton trials",
    "why": "at batch=128 the engine alone holds ~29-30 GiB; the two services cannot coexist on this card, and co-residency would contaminate memory headroom and timing"
  },
  "tooling": {
    "harness": "tools/validation/perf_matrix_sdk.py (built on the qwen3tts client SDK)",
    "orchestration": "tools/validation/run_perf_matrix.sh",
    "summarizer": "tools/validation/summarize_perf_matrix.py (per trial), pooled across trials with identical aggregation logic"
  },
  "trial_design": {
    "engine_trials": 3,
    "triton_trials": 3,
    "engine_concurrency_levels": [1, 8, 16, 32, 64, 128],
    "triton_concurrency_levels": [1, 16, 32, 64, 128],
    "measured_rounds_per_level": 20,
    "warmup_rounds_per_level": 3,
    "connection_modes": ["reuse", "cold"],
    "connection_mode_measured_rounds": 50,
    "connection_mode_warmup_rounds": 5,
    "engine_trial_ids": ["bf16refresh-engine-01", "bf16refresh-engine-02", "bf16refresh-engine-03"],
    "total_requests": 51981,
    "engine_requests": 35022,
    "triton_requests": 16959,
    "failures": 0,
    "aggregation_note": "summary CSVs pool every recorded row of a group, including the phase=warmup rounds (same aggregation as the 2026-07-06 dataset, so cross-dataset deltas are apples-to-apples); warmup rows shift level-1 averages by well under 1 ms and high-concurrency averages by ~1%. Filter raw_requests.csv on phase=measure to exclude them."
  },
  "supplementary_measurements": {
    "server_ttft_single_stream_warm_ms": {
      "basis": "server_session_create_to_first_raw_audio_ms from the done-event meta (same basis as the historical '13ms' claim), re-measured 2026-07-08 on this rebuild",
      "n": 50,
      "min": 14.41,
      "p50": 14.80,
      "avg": 14.89,
      "stdev": 0.34,
      "p99": 15.85,
      "max": 16.25,
      "note": "statistically unchanged vs the 2026-07-07/08 measurements (avg 14.92-14.96) -- the round-3 commits and the rebuild do not touch the single-stream path"
    },
    "burst_ramp_decomposition": {
      "tool": "workspace/ttft_ramp_decompose.py, 128 concurrent sessions, shared gRPC channel, warm engine",
      "runs": 2,
      "client_ttft_avg_ms": [285.8, 287.9],
      "note": "per-stage table in the report's 'Where burst TTFT goes' section; measured 2026-07-07 on the round-2 build (predates cc58c6a/8596996/b80c38d) and RETAINED -- the mechanism analysis still applies, headline TTFT has since settled at grpc ~275ms / ws ~242ms"
    }
  }
}
