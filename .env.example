MODEL_VARIANT=custom-1.7b

# Standalone engine runtime mounts
ENGINE_MODELS_DIR=./workspace/models
ENGINE_EXPORTED_DIR=./workspace/exported

# Triton runtime mounts
TRITON_MODEL_REPO_DIR=./workspace/model_repository

# Ports
ENGINE_GRPC_PORT=50051
ENGINE_WEBSOCKET_PORT=50052
ENGINE_HEALTH_PORT=8080
TRITON_HTTP_PORT=8000
TRITON_GRPC_PORT=8001
TRITON_METRICS_PORT=8002

# Built-in Demo is served on ENGINE_WEBSOCKET_PORT. Plain HTTP/WS is the
# default even when workspace/tls contains local certificates.
DEMO_ENABLED=true
TLS_AUTO_ENABLE=false
# To opt into direct HTTPS/WSS with tools/generate_demo_local_cert.sh:
# TLS_AUTO_ENABLE=true
# For CA-issued certificates instead, set all three explicitly:
# TLS_HOST_DIR=/host/path/to/certificate
# TLS_CERT_FILE=/app/tls/fullchain.pem
# TLS_KEY_FILE=/app/tls/privkey.pem

# Optional runtime tuning
ENGINE_DEVICE=0
ENGINE_MAX_BATCH_SIZE=64
ENGINE_MAX_SESSIONS=128
# /health probe semantics: ready (503 until engine ready, default) | alive (always 200)
# ENGINE_HEALTH_PROBE_MODE=ready
TRITON_MAX_BATCH_SLOTS=64
TRITON_MAX_SESSIONS=128
